import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'inventory_management_service',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
